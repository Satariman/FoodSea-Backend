linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - gosimple
    - unused

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-dirs:
    - ent
    - docs
